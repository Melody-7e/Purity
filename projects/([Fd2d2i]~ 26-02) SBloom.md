# Ri Icon

> Full implementation is available at my _rili_ project.

# Main Function

$$g(x)    = \text{lerp}\big(x, x^2, \frac{k}{2}\big)$$
$$f(a, b) = g\big(a + k\big[\text{lerp}\big((a^{e^z} - a), (1 - a), b\big)\big]\big)$$
